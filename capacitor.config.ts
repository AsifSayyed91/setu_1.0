import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mulkeHealth.patienteHealth',
  appName: 'Mulk Med',
  webDir: 'www',
  bundledWebRuntime: false


};

export default config;
